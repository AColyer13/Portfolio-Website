---
layout: default
---

# Portfolio placeholder

The live site is the **Vite + React** app built by `.github/workflows/deploy.yml` in this repository.

If you do not need Jekyll at all, delete the **pages build and deployment** workflow in the **Actions** tab and set **Settings → Pages → Source** to **GitHub Actions** → **Deploy Vite site to GitHub Pages** only.
